#!/bin/zsh

window=$(wmctrl -l|awk '{print $0}' | dmenu -i -l 5 -p "Windows")
if [ "$window"!="" ]
then
	desktop="$(cut -d " " -f3 <<< "$window")"
	id="$(cut -d " " -f1 <<< "$window")"
	wmctrl -s "$desktop" -a "$id" -i
fi
